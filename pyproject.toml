[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "padding-oracle-python"
version = "0.1.0"
description = "CBC padding-oracle attack project in Python (tasks 2, 3, 4)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "cryptography>=42.0.0",
]
